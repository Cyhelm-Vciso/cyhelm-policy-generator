[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cyhelm-virtual-ciso-policy-generator"
version = "0.1.0"
description = "Deterministic, review-first information security policy generator"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115,<1", "uvicorn[standard]>=0.30,<1"]

[project.optional-dependencies]
dev = ["httpx>=0.27,<1", "pytest>=8,<9", "ruff>=0.8,<1"]

[tool.hatch.build.targets.wheel]
packages = ["src/cyhelm"]

[tool.pytest.ini_options]
pythonpath = ["src"]

